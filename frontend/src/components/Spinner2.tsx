import { Spinner } from "@fluentui/react-components"

const Spinner2 = () => {
    return (
        <Spinner
            size="huge"
            className="rounded-full bg-gradient-to-r from-green-900 via-blue-500 to-red-900 animate-spin"
        />
    )
}

export default Spinner2